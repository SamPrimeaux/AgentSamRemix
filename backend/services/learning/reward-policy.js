/**
 * Compatibility export for callers that have not moved to the AI routing
 * training domain yet. The canonical reward policy lives there.
 */
export * from '../../ai/routing/training/reward-policy.js';
