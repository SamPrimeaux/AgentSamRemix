{
  "name": "agentsamremix-terminal-daemon",
  "version": "0.0.1",
  "private": true,
  "description": "Local/VM PTY daemon for AgentSamRemix's localpty and vm terminal lanes. Runs as a separate Node process — Cloudflare Workers cannot spawn PTYs, so the Worker's /api/terminal/* routes proxy to this.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "node scripts/fix-node-pty-perms.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "node-pty": "^1.1.0",
    "ws": "^8.20.1"
  }
}
