# CMS Pipeline

Portable orchestration for ordered CMS lifecycle operations. Host adapters inject storage, gate, assembly, telemetry and cache capabilities. The pipeline owns sequencing and failure/lock semantics; it does not import provider SDKs, Cloudflare bindings, or customer-specific implementations.
