# CMS Lifecycle

Portable lifecycle policy for drafts, publication state, revisions, archive/restore and purge eligibility. Full-page artifact revisions normalize the existing rollback store; structured override revisions normalize `cms_override_versions`.

Cloudflare bindings, R2 copies, D1 SQL, publish gates, host-specific assembly and editor behavior do not belong here.
