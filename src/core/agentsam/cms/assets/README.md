# CMS Assets

Canonical CMS asset identity, metadata, type/kind classification, usage context and collection membership.

The domain does not know D1 column names, Cloudflare binding names, bucket routing, presigning, or public-domain rules. Physical `cms_assets` schema drift is absorbed by the D1 adapter. R2 object transport is an adapter concern.
