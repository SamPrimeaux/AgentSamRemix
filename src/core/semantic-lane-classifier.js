/**
 * semantic-lane-classifier — GUTTED (pre-LLM nuke 2026-08).
 * No message → semantic lane. Model calls retrieve tools from the mode menu.
 */

/** @deprecated Always null. */
export function classifySemanticLane(_message) {
  return null;
}

/** @deprecated Always false. */
export function shouldSupplementDeepArchive(_message, _lane) {
  return false;
}

/** @deprecated Always null. */
export function classifyDatabaseAssistantIntent(_message) {
  return null;
}
